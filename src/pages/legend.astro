---
import Layout from '../layouts/Layout.astro';

const base = import.meta.env.BASE_URL;

function getLink(path: string) {
  if (path === '') return base;
  return base.endsWith('/') ? `${base}${path}/` : `${base}/${path}/`;
}
---

<Layout title="Режим LEGEND - Влад Хелс-коуч">
	<section class="legend-hero">
		<div class="container">
			<div class="back-navigation">
				<a href={getLink('services')} class="back-button">
					<span class="back-icon">←</span>
					<span>Назад к услугам</span>
				</a>
			</div>
			<div class="legend-header">
				<h1>Режим: LEGEND</h1>
				<div class="price-info">
					<div class="price">72 000 € за 4 месяца</div>
					<div class="duration">(перелёты и проживание включены)</div>
				</div>
			</div>
		</div>
	</section>

	<section class="legend-content">
		<div class="container">
			<div class="content-wrapper">
				<div class="main-description">
					<p class="intro-text">На 4 месяца я становлюсь частью твоей жизни. Когда нужен не план, а человек, который живёт рядом с тобой и ведёт к результату. Максимально личный и глубокий формат работы.</p>
					<p class="additional-info">Включает всё то же, что и стандартное сопровождение, а способ контроля питания мы выбираем на месте.</p>
				</div>

				<div class="what-awaits">
					<h2>Что тебя ждёт:</h2>
					
					<div class="highlight-section">
						<h3>Две недели каждого месяца я живу рядом с тобой и лично веду тебя:</h3>
					</div>
					
					<div class="benefits-list">
						<div class="benefit-item">
							<div class="benefit-icon">🛒</div>
							<p>ходим в магазины — показываю, какие продукты и бренды брать;</p>
						</div>
						
						<div class="benefit-item">
							<div class="benefit-icon">🍽️</div>
							<p>ходим в рестораны/кафе, где я научу тебя ориентироваться в меню с учётом твоих предпочтений и целей;</p>
						</div>
						
						<div class="benefit-item">
							<div class="benefit-icon">🏋️</div>
							<p>тренируемся вместе в твоём зале — я твой личный тренер на месте, контролирую, поддерживаю и делюсь секретами GYM-комьюнити;</p>
						</div>
						
						<div class="benefit-item">
							<div class="benefit-icon">🚶</div>
							<p>сопровождаю тебя на вечерних прогулках, формируем привычки в реальной жизни;</p>
						</div>
						
						<div class="benefit-item">
							<div class="benefit-icon">🔧</div>
							<p>разбираем и корректируем каждую мелочь, которая может мешать твоему результату;</p>
						</div>
						
						<div class="benefit-item">
							<div class="benefit-icon">📱</div>
							<p>в остальные две недели месяца — дистанционное сопровождение и контроль, чтобы ты не сбивался с ритма, а также созвон раз в неделю.</p>
						</div>
					</div>
					
					<div class="special-note">
						<p>Это не просто коучинг — это прямое погружение в твой образ жизни с полной перезагрузкой пищевого поведения и тренировочных привычек.</p>
					</div>
					
					<div class="additional-costs">
						<p><em>Расходы на еду, рестораны и зал — оплачиваются отдельно.</em></p>
					</div>
				</div>

				<div class="action-section">
					<div class="cta-buttons">
						<button onclick="openBookingModal('Режим: LEGEND - Личное сопровождение')" class="cta-button primary">Записаться</button>
					</div>
				</div>
			</div>
		</div>
	</section>
</Layout>

<style>
	.legend-hero {
		background: var(--text-color);
		color: white;
		padding: 60px 0 60px;
		position: relative;
	}

	.container {
		max-width: 1000px;
		margin: 0 auto;
		padding: 0 20px;
	}

	.back-navigation {
		margin-bottom: 30px;
	}

	.back-button {
		display: inline-flex;
		align-items: center;
		gap: 10px;
		color: white;
		text-decoration: none;
		font-size: 1.1rem;
		font-weight: 400;
		padding: 10px 20px;
		border: 1px solid rgba(255, 255, 255, 0.5);
		border-radius: 2px;
		transition: opacity 0.3s ease;
		background: rgba(255, 255, 255, 0.1);
	}

	.back-button:hover {
		opacity: 0.8;
	}

	.back-icon {
		font-size: 1.3rem;
	}

	.legend-header {
		text-align: center;
	}

	.legend-header h1 {
		font-size: 3rem;
		font-weight: 300;
		margin-bottom: 20px;
	}

	.price-info {
		margin-bottom: 20px;
	}

	.price {
		font-size: 2rem;
		font-weight: 300;
		margin-bottom: 5px;
		color: var(--accent-copper);
	}

	.duration {
		font-size: 1.1rem;
		opacity: 0.9;
	}

	.legend-content {
		padding: 80px 0;
		background: white;
	}

	.content-wrapper {
		max-width: 800px;
		margin: 0 auto;
	}

	.main-description {
		margin-bottom: 50px;
	}

	.intro-text {
		font-size: 1.3rem;
		line-height: 1.7;
		color: var(--text-color);
		text-align: center;
		padding: 30px;
		background: var(--bg-warm);
		border-radius: 2px;
		border-left: 2px solid var(--accent-copper);
		margin-bottom: 20px;
	}

	.additional-info {
		font-size: 1.1rem;
		line-height: 1.6;
		color: var(--text-light);
		text-align: center;
		font-style: italic;
		margin: 0;
	}

	.what-awaits h2 {
		font-size: 2.2rem;
		font-weight: 300;
		color: var(--accent-copper);
		margin-bottom: 30px;
		text-align: center;
	}

	.highlight-section {
		background: white;
		padding: 25px;
		border-radius: 2px;
		margin-bottom: 40px;
		border: 1px solid var(--accent-copper);
	}

	.highlight-section h3 {
		font-size: 1.4rem;
		font-weight: 400;
		color: var(--accent-copper);
		margin: 0;
		text-align: center;
	}

	.benefits-list {
		margin-bottom: 40px;
	}

	.benefit-item {
		display: flex;
		align-items: flex-start;
		gap: 20px;
		padding: 20px;
		margin-bottom: 15px;
		background: white;
		border-radius: 2px;
		border: 1px solid var(--border-color);
		border-left: 2px solid var(--accent-copper);
		transition: opacity 0.3s ease;
	}

	.benefit-item:hover {
		opacity: 0.95;
	}

	.benefit-icon {
		font-size: 1.8rem;
		flex-shrink: 0;
		width: 40px;
		text-align: center;
	}

	.benefit-item p {
		margin: 0;
		line-height: 1.6;
		color: var(--text-color);
		font-size: 1.1rem;
	}

	.special-note {
		background: var(--text-color);
		color: white;
		padding: 25px;
		border-radius: 2px;
		margin-bottom: 30px;
		text-align: center;
	}

	.special-note p {
		margin: 0;
		font-size: 1.2rem;
		font-weight: 400;
		font-style: italic;
		line-height: 1.6;
	}

	.additional-costs {
		text-align: center;
		margin-bottom: 50px;
	}

	.additional-costs p {
		color: var(--text-light);
		font-size: 0.95rem;
		margin: 0;
	}

	.action-section {
		text-align: center;
		padding-top: 30px;
		border-top: 2px solid var(--bg-warm);
	}

	.cta-buttons {
		display: flex;
		gap: 20px;
		justify-content: center;
		flex-wrap: wrap;
	}

	.cta-button {
		padding: 15px 30px;
		border: 1px solid var(--text-color);
		border-radius: 2px;
		font-size: 1.1rem;
		font-weight: 500;
		text-transform: uppercase;
		letter-spacing: 1px;
		cursor: pointer;
		transition: opacity 0.3s ease;
		text-decoration: none;
		display: inline-block;
		text-align: center;
		min-width: 180px;
	}

	.cta-button.primary {
		background: var(--text-color);
		color: white;
		border: 1px solid var(--text-color);
	}

	.cta-button.primary:hover {
		opacity: 0.9;
	}

	.cta-button.secondary {
		background: transparent;
		color: var(--text-color);
		border: 1px solid var(--text-color);
	}

	.cta-button.secondary:hover {
		opacity: 0.9;
	}

	@media (max-width: 768px) {
		.legend-hero {
			padding: 60px 0 40px;
		}

		.legend-header h1 {
			font-size: 2.2rem;
			letter-spacing: 0;
		}

		.price {
			font-size: 1.6rem;
		}

		.duration {
			font-size: 1rem;
		}

		.intro-text {
			font-size: 1.1rem;
			padding: 20px;
		}

		.what-awaits h2 {
			font-size: 1.8rem;
		}

		.benefit-item {
			flex-direction: column;
			gap: 10px;
			text-align: center;
		}

		.benefit-icon {
			align-self: center;
		}

		.cta-buttons {
			flex-direction: column;
			align-items: center;
		}

		.cta-button {
			width: 100%;
			max-width: 300px;
		}

		.back-button {
			font-size: 1rem;
			padding: 8px 16px;
		}
	}
</style>

<script is:inline>
	// Booking modal function is already defined in Layout.astro
	// function openBookingModal() is available globally
</script>